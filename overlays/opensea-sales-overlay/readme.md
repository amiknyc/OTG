# OTG OpenSea Sales Overlay

OBS browser overlay that shows recent **OpenSea sale events** for the  
**Off The Grid** collection, powered by a **Vercel serverless proxy** so your OpenSea API key stays server-side.

This project lives inside the broader `otg` monorepo under:

```text
overlays/opensea-sales-overlay/
